# Глава 30: Скрытые затраты

Затраты на хранилище отображаются одной строкой: «S3: $198». Затраты на вычисления — одной строкой: «EC2: $2 340». Затраты на сеть разбросаны по дюжине строк с названиями вроде «Data Transfer Out», «NAT Gateway Processing», «VPC Peering Data Transfer» и «CloudFront Data Transfer». Большинство инженеров суммируют их один раз, моргают и суммируют снова.

Tom сказал: «Сетевые затраты. Это следующее».

Он открыл счёт. Нашёл раздел передачи данных. Сложил все строки.

Сетевые затраты в AWS похожи на городскую систему платных дорог: въезд в город бесплатен, но каждый туннель на выезде стоит денег, и поездка между районами тоже немного стоит. Большинство людей не думают о пошлинах, пока в конце месяца не получают счёт и не понимают, что каждый день проезжали через туннель, когда рядом всё время была бесплатная наземная дорога. Цель этой главы — понять каждый пункт оплаты — и решить, какие из них стоит платить.

$847/мес.

— Мы тратим $847 в месяц на передачу данных, — сказал он.

— Это много? — спросил Leo.

— Это больше, чем наш счёт S3 до оптимизации. И я даже не знал, что у нас такой счёт за передачу данных.

Maya взглянула. — Что именно такое передача данных?

— Это то, что AWS берёт за перемещение байт. Байты в AWS: обычно бесплатно. Байты из AWS в интернет: платные. Байты между сервисами в разных регионах: платные. Байты, проходящие через NAT Gateway: платные.

— Можете разбить подробнее?

Tom мог. И то, что он обнаружил, изменило представление команды об их архитектуре.

**Как AWS начисляет плату за передачу данных**

Ценообразование AWS на передачу данных асимметрично:

**В AWS (входящий)**: бесплатно. Вы можете загружать сколько угодно данных.

**Из AWS в интернет (исходящий)**: платно. Первые 100 ГБ/мес. бесплатны. После:

- $0,09/ГБ за первые 10 ТБ/мес. (регионы US)
- $0,085/ГБ за следующие 40 ТБ
- Ниже при больших объёмах

**Внутри той же зоны доступности**: бесплатно. EC2-инстансы, взаимодействующие в той же AZ, ничего не платят.

**Между зонами доступности (один регион)**: $0,01/ГБ в каждом направлении. Небольшая, но реальная стоимость.

**Между регионами**: $0,02–0,08/ГБ в зависимости от регионов. Кросс-региональный трафик значительно дороже.

**NAT Gateway**: $0,045/ГБ обработки. Каждый байт, отправляемый вашим частным EC2-инстансом через NAT Gateway для выхода в интернет — и каждый байт, возвращающийся — тарифицируется.

**CloudFront**: более низкие тарифы на передачу данных, чем прямое AWS-в-интернет. $0,085/ГБ за первые 10 ТБ (немного меньше, чем прямая исходящая передача данных). CloudFront часто снижает общие затраты на передачу, потому что кэширование на граничных серверах означает, что источник отдаёт данные реже.

**Разбивка Tom**

После категоризации каждой строки:

**Исходящие данные в интернет**: $214/мес.

- Ответы API клиентам по всему миру
- Заполнение кэша CloudFront (когда граничные серверы запрашивают источник)

**Обработка NAT Gateway**: $289/мес.

- Серверы приложений, вызывающие внешние API (платёжный процессор, сервис электронной почты, данные карт)
- Вызовы DynamoDB, проходящие через NAT Gateway (до настройки VPC Endpoints для некоторых таблиц)

**Кросс-AZ передача данных**: $178/мес.

- Балансировщик нагрузки к EC2-инстансам (балансировщик в одной AZ, некоторые инстансы в другой)
- Сервер приложений к реплике RDS для чтения (в другой AZ)

**Кросс-региональная передача данных**: $166/мес.

- Репликация Aurora Global Database (основная в us-east-1, читатель в us-west-2)
- Кросс-региональная репликация S3 для резервных копий

**NAT Gateway: наибольший сюрприз**

$289/мес. в сборах за обработку NAT Gateway были самой большой статьёй. И они были частично ненужными.

В главе 11 Tom настроил VPC Gateway Endpoints для S3 и DynamoDB. Они были бесплатными. Но он пропустил настройку Interface Endpoints для нескольких других сервисов:

- Systems Manager (SSM) для управления патчами
- Secrets Manager для получения учётных данных
- CloudWatch для отправки метрик и логов
- SQS для опроса сообщений

Каждый вызов к этим сервисам от частных EC2-инстансов проходил через NAT Gateway. Каждый вызов тарифицировался по $0,045/ГБ.

**Interface Endpoints** для этих сервисов: $0,01/час на AZ + $0,01/ГБ обработанных данных.

При объёме Nimbus SSM Interface Endpoint стоил бы около $15/мес. и экономил бы около $43/мес. в платах NAT Gateway (потому что SSM генерирует значительный объём данных для управления патчами и вызовов хранилища параметров).

Стоимость и экономия от Endpoint варьировались по сервисам и объёму. Tom подсчитал, что настройка Interface Endpoints для четырёх высокотрафиковых сервисов обошлась бы $62/мес. в совокупности и сэкономила бы примерно $140/мес. в обработке NAT Gateway.

Чистая экономия: $78/мес. только от настройки Endpoints.

**Кросс-AZ трафик: архитектурный вопрос**

$178/мес. кросс-AZ трафика было сложнее.

Часть была неизбежна: балансировщик нагрузки распределяет трафик по AZ, поэтому некоторые запросы поступают в одну AZ, и балансировщик направляет их к инстансу в другой AZ.

Часть была оптимизируема: приложение было настроено на запись к основной RDS (в us-east-1a) и чтение из реплики для чтения (в us-east-1b). Каждый запрос чтения пересекал границы AZ.

Для чтений одно решение: настроить приложение на предпочтение реплики для чтения в той же AZ, что и запрашивающий инстанс. Каждая AZ получает собственную реплику для чтения. Трафик остаётся локальным.

Компромисс: больше реплик для чтения = больше затрат. Если кросс-AZ трафик стоит $50/мес., а дополнительная реплика для чтения — $190/мес., AZ-локальная оптимизация не окупается.

Tom рассчитал: при их текущем объёме запросов кросс-AZ трафик был всего $31/мес. от $178. Не стоит добавлять реплики ради этого.

Другие кросс-AZ затраты были от маршрутизации балансировщика нагрузки и межсервисной коммуникации — в значительной мере неизбежные на текущем архитектурном уровне.

— Это один из тех случаев, когда понимание затрат не означает, что нужно их исправлять, — сказал Tom.

— Сколько стоит полностью устранить кросс-AZ трафик? — спросила Maya.

— Всё в одной AZ лишает смысла Multi-AZ. Это экономия $31/мес. ценой потери высокой доступности.

— Значит, оставляем, — сказала она.

— Оставляем.

Это зрелый разговор о затратах: иногда вы платите за что-то, потому что альтернатива стоит дороже в виде рисков.

**CloudFront: скидка на передачу данных**

Вот контринтуитивный факт: обслуживание данных через CloudFront, как правило, дешевле, чем обслуживание напрямую с EC2 или S3.

**Прямое EC2 в интернет**: $0,09/ГБ
**CloudFront в интернет**: $0,085/ГБ (немного дешевле)

Но реальная экономия не в ставке за ГБ — а в том, что CloudFront кэширует данные на граничных серверах. Если 1 000 пользователей запрашивают одну и ту же фотографию меню:

- **Без CloudFront**: 1 000 запросов к источнику S3 × размер фото × $0,09/ГБ
- **С CloudFront**: 1 запрос к S3 (промах кэша) + 999 запросов, обслуживаемых из граничного кэша по тарифам CloudFront

Для Nimbus с 83% частотой попаданий в кэш (из главы 13) они обслуживали 83% запросов из граничного кэша. Фактическая передача данных с источника составляла 17% от общего числа запросов — 83% их «исходящего» трафика кэшировалось на граничном сервере.

— CloudFront — это не только CDN для производительности, — сказал Tom. — Это также оптимизация затрат на передачу данных.

Leo задумался. — Нам следует пропускать весь контент статических ресурсов через CloudFront, даже для ресурсов, не чувствительных к задержке.

— Верно. Если пользователи скачивают это из AWS, это должно идти через CloudFront.

**S3 Select: сокращение передачи данных в запросах**

Тонкая оптимизация: **S3 Select** позволяет получать только нужные строки и столбцы из объекта S3 (CSV, JSON, Parquet), не скачивая весь файл для фильтрации в вашем приложении.

Без S3 Select:
```python
# Скачать файл 500 МБ, обработать в памяти
data = s3.get_object(Bucket='analytics', Key='orders-2024.csv')
df = pd.read_csv(data['Body'])
result = df[df['restaurant_id'] == '47'][['order_id', 'total']]
```

С S3 Select:
```python
# Пусть S3 сначала отфильтрует, передать только совпадающие строки (~2 МБ вместо 500 МБ)
response = s3.select_object_content(
    Bucket='analytics',
    Key='orders-2024.csv',
    Expression="SELECT order_id, total FROM S3Object WHERE restaurant_id = '47'"
)
```

S3 Select сокращает объём данных, передаваемых из S3 в ваше приложение. Для больших файлов с выборочными запросами это может дать снижение объёма данных в 10–100 раз — и, следовательно, стоимости.

**Полная сетевая оптимизация**

После трёх недель анализа и реализации:

| Статья затрат                                       | До       | После    | Ежемесячная экономия |
|-----------------------------------------------------|----------|----------|----------------------|
| NAT Gateway (Interface Endpoints)                   | $289     | $211     | $78                  |
| Оптимизация CloudFront (перенос большего числа ресурсов) | $214 | $147     | $67                  |
| Кросс-AZ трафик (принято как есть)                  | $178     | $178     | $0                   |
| Кросс-региональный трафик (принято как есть)        | $166     | $166     | $0                   |
| **Итого**                                           | **$847** | **$702** | **$145/мес.**        |

$145/мес., $1 740/год экономии на сети. Скромно по сравнению с вычислениями и хранилищем, но ощутимо.

Важнее: Tom теперь понимал каждую строку сетевого счёта. Он мог объяснить каждую статью затрат и сознательно решил, что оптимизировать, а что принять.

## Преимущества и ограничения

**Затраты NAT Gateway**:

- Большие объёмы данных через NAT Gateway быстро накапливаются
- VPC Endpoints полностью устраняют часть затрат NAT
- Проверьте, какие сервисы вызывают ваши частные инстансы и доступны ли для них Endpoints

**CloudFront для оптимизации затрат**:

- Частота попаданий в кэш напрямую определяет экономию
- Высокая частота попаданий = меньше передачи с источника + меньше общих затрат на передачу
- Пропускайте всю доставку статических ресурсов через CloudFront

**Компромиссы кросс-AZ**:

- Устранение кросс-AZ трафика обычно требует архитектурных изменений, которые стоят дороже экономии
- Тщательно рассчитывайте перед оптимизацией

**S3 Select**:

- Значительная экономия для выборочных запросов к большим объектам S3
- Не помогает, когда нужен весь файл

В следующей главе: шестистолпный фреймворк, который задаёт вопросы, с которых должен начинаться каждый архитектурный анализ.

## Резюме

- AWS взимает плату за **исходящие данные** (интернет: ~$0,09/ГБ), **кросс-AZ трафик** ($0,01/ГБ в каждом направлении), **кросс-региональный трафик** ($0,02–0,08/ГБ) и **обработку NAT Gateway** ($0,045/ГБ).
- **Входящие данные** бесплатны. **Трафик внутри AZ** бесплатен.
- **VPC Gateway Endpoints** (S3, DynamoDB): бесплатно. Устраняют затраты NAT Gateway для этих сервисов.
- **VPC Interface Endpoints**: платные — за час плюс за ГБ. Дешевле NAT Gateway для высокообъёмных сервисов.
- **CloudFront** обслуживает данные по более низким тарифам, чем прямое EC2-в-интернет, и резко снижает объём передачи с источника через кэширование.
- **S3 Select** сокращает передачу данных из S3 путём фильтрации у источника.
- Некоторые затраты на сеть — архитектурные компромиссы (кросс-AZ для HA) — понимайте их, но не всегда устраняйте.

## Советы к экзамену

*SAA-C03, область: проектирование архитектур с оптимизацией затрат (область 4, задача 4.4)*

- **NAT Gateway и VPC Endpoints**: сценарий экзамена: «EC2 в частной подсети часто вызывает S3/DynamoDB — как снизить затраты NAT Gateway?» → VPC Gateway Endpoints (бесплатно для S3 и DynamoDB).
- **Правила ценообразования на передачу данных**:
  - В AWS: бесплатно
  - Внутри AZ: бесплатно
  - Между AZ: платно
  - Между регионами: платно (по более высокой ставке)
  - Интернет: платно (значительная ставка)
- **CloudFront как оптимизация затрат**: «снизить затраты на передачу данных для глобальной доставки контента» → CloudFront. Уровень кэша снижает количество запросов к источнику.
- **S3 Transfer Acceleration**: ускоряет загрузки *в* S3 с использованием граничных серверов CloudFront. Выше стоимость, чем стандартный S3. Используйте для клиентов, загружающих большие файлы из географически удалённых мест.
- **Стоимость кросс-региональной репликации**: репликация данных между регионами влечёт затраты на передачу данных. Для S3 CRR вы платите и тариф на исходящую передачу, и стоимость запроса S3.
- **PrivateLink (VPC Interface Endpoints)**: обеспечивает частное подключение к сервисам AWS и к сервисам, размещённым другими клиентами AWS. Более безопасно, чем через NAT, часто дешевле для высокообъёмных сервисов.

## Упражнения

**Упражнение 1 — Воспроизведение**

Объясните разницу между VPC Gateway Endpoint и VPC Interface Endpoint. Для каких сервисов AWS доступен каждый и какова их стоимость?

*(Подсказка: Gateway Endpoints бесплатны, но только для S3 и DynamoDB. Interface Endpoints стоят за час, но работают для большинства других сервисов AWS.)*

**Упражнение 2 — Подготовка к экзамену**

*Сценарий*: Приложение компании работает на EC2-инстансах в частных подсетях. Инстансы делают частые вызовы API к Amazon SQS и Amazon S3. В настоящее время весь трафик выходит через NAT Gateway. Команда хочет снизить затраты NAT Gateway. Безопасность данных должна сохраняться — никакой трафик не должен проходить через публичный интернет.

Какой подход наилучшим образом отвечает этим требованиям с минимальными текущими затратами?

A) Создать Gateway Endpoint для SQS и Gateway Endpoint для S3  
B) Создать Interface Endpoint для SQS и Gateway Endpoint для S3  
C) Создать Interface Endpoints для SQS и S3  
D) Удалить NAT Gateway и использовать Internet Gateway напрямую для вызовов API

**Подсказка 1**: Gateway Endpoints доступны только для S3 и DynamoDB.

**Подсказка 2**: Interface Endpoints доступны для SQS и многих других сервисов (но стоят денег).

**Подсказка 3**: Internet Gateway в таблице маршрутизации частной подсети сделает её публичной — нарушая требования безопасности.

**Ответ**: B

**Пояснение**: S3 использует Gateway Endpoint (бесплатно). SQS требует Interface Endpoint (платный). Это сочетание устраняет затраты на обработку NAT Gateway для обоих сервисов. Весь трафик остаётся внутри частной сети AWS — без прохождения через публичный интернет.

**Почему не A?** Gateway Endpoints недоступны для SQS. Только S3 и DynamoDB имеют Gateway Endpoints.

**Почему не C?** Хотя это работает, использование Interface Endpoint для S3 (вместо бесплатного Gateway Endpoint) влечёт ненужные почасовые сборы. Всегда используйте бесплатный Gateway Endpoint для S3 и DynamoDB.

**Почему не D?** Добавление маршрута к Internet Gateway из частной подсети делает её публичной подсетью. EC2-инстансы в частных подсетях, как правило, не имеют Elastic IP, поэтому они не могут маршрутизировать через Internet Gateway без дополнительных изменений — и это подвергнет их входящему интернет-трафику.

*SAA-C03, область: проектирование архитектур с оптимизацией затрат — задача 4.4*

**Упражнение 3 — Архитектурный вызов** *(Дополнительно)*

Пользователи Nimbus с Западного побережья генерируют значительный трафик. Приложение обслуживает их из us-east-1 (Вирджиния). В настоящее время:

- Ответы API идут напрямую от EC2-инстансов us-east-1 пользователям Западного побережья (~80 мс, $0,09/ГБ)
- Фотографии меню идут из S3 us-east-1 через граничный сервер CloudFront в Сиэтле (~8 мс после кэширования)

Команда рассматривает добавление второго региона приложения в us-west-2 (Орегон) для пользователей Западного побережья с целью снижения задержки API.

Проанализируйте затраты на передачу данных при этом изменении. Какие новые кросс-региональные затраты на передачу данных создаст двухрегиональная конфигурация? Снизит или увеличит маршрутизация Route 53 на основе задержки общие затраты на передачу? При каких условиях (объём трафика, чувствительность к задержке) двухрегиональная конфигурация окупится?

*(Единственно правильного ответа не существует. Цель — практика многорегионального анализа затрат и выгод.)*

## Сцена после титров

Tom закрыл сетевой анализ.

Общий эффект проекта оптимизации за три месяца:

- EC2 Savings Plans: -$14 200/год
- Хранилище (S3 + EBS): -$6 200/год
- Уровень базы данных: -$11 220/год
- Сеть: -$1 740/год
- **Итого: -$33 360/год**

Он написал это на доске в комнате для совещаний.

Leo уставился на это. — Тридцать три тысячи.

— С хвостиком, — сказал Tom.

— В год.

— В год.

Priya посчитала. — Это $2 780 в месяц, которые мы тратили на то, что не создавало ценность.

— Не всё, — поправил Tom. — Часть из этого — вещи, от которых мы получали ценность, но за которые платили слишком много. Savings Plans — мы получали точно те же мощности EC2, просто по лучшей цене.

Maya долго стояла у доски.

— Когда мы начинали Nimbus, — сказала она, — каждый доллар имел значение. Мы едва могли позволить себе первый EC2-инстанс.

— Да, — сказал Tom.

— И где-то по пути мы перестали следить за долларами так же тщательно.

— Рост делает это, — сказала Priya. — Фокус смещается на строительство, а не на оптимизацию.

— Важно и то, и другое, — сказала Maya. — Всегда оба. Добавьте это в вики. И установите ежеквартальную проверку затрат.

Tom уже открывал свой календарь.

В следующих главах: мы отдаляемся от отдельных сервисов и начинаем думать как архитекторы.
