# Глава 20: Модель фрилансера

Функция уведомления о заказе выполнялась ровно один раз на каждый заказ. Между заказами она ничего не делала. Семнадцать часов в один из вторников не поступало ни одного заказа. За эти семнадцать часов функция не стоила ни копейки. Ни одного цента. Никакого простаивающего сервера, никакого ожидающего инстанса, никакой зарезервированной мощности, которая бы не использовалась. Функция существовала. Она просто не работала.

Веерная рассылка через SNS/SQS работала. Сервис аналитики, сервис уведомлений и сервис электронной почты — каждый получал данные из своей очереди SQS.

Но Priya кое-что заметила.

— Сервис электронной почты, — сказала она. — Сколько писем мы отправляем в час?

Leo проверил метрики. — В среднем 400. В пиковые часы — около 1 200 в пятничные вечера.

— А EC2-инстанс, на котором работает сервис электронной почты, — как долго он работает?

— Постоянно. Круглосуточно, семь дней в неделю.

— Даже в 3 ночи, когда мы не отправляем ни одного письма?

Тишина.

— Мы платим за компьютер, который сидит и ничего не делает, — сказал Leo.

— Сколько часов в день?

Снова тишина.

— Около 18.

Tom теперь слушал очень внимательно.

**Сервер — не всегда лучший ответ**

EC2-инстансы постоянны. Вы запускаете инстанс, и он работает до тех пор, пока вы его не остановите — 24 часа в сутки, 7 дней в неделю, независимо от реальной нагрузки. Для веб-сервера, который обрабатывает трафик в любое время суток, это правильно. Для сервиса электронной почты, который отправляет письма всплесками, а потом простаивает часами, — это расточительство.

Auto Scaling Group может масштабировать сервис электронной почты до одного инстанса в нерабочее время. Но один инстанс всё равно работает постоянно.

Что если код запускался бы только тогда, когда есть работа?

В этом и состоит суть **бессерверных вычислений**.

**AWS Lambda: код без серверов**

**AWS Lambda** позволяет запускать код в ответ на события, не выделяя серверы и не управляя ими. Вы загружаете функцию, указываете, что её запускает, и Lambda выполняет её при срабатывании триггера.

Lambda-функция:

- Не имеет постоянного состояния (каждый вызов независим)
- Выполняется не дольше 15 минут за один вызов
- Масштабируется автоматически от 0 до тысяч одновременных вызовов
- Тарифицируется только во время выполнения (за каждую 1 мс работы, округлённую вверх, на каждый ГБ выделенной памяти)

Когда триггеров нет, Lambda ничего не стоит. Когда триггеры срабатывают, Lambda работает и начисляет плату. Когда одновременно срабатывают 10 000 триггеров, Lambda запускает 10 000 параллельных вызовов. Масштабирование — автоматическое и почти мгновенное.

**Триггеры событий: что пробуждает Lambda**

Lambda-функции не работают сами по себе — они реагируют на события. Распространённые триггеры:

- **Очередь SQS**: обработка сообщений из очереди. Lambda опрашивает очередь и вызывает функцию с пакетами сообщений.
- **API Gateway**: поступает HTTP-запрос. API Gateway запускает Lambda. Lambda формирует ответ.
- **Событие S3**: файл загружается в S3. Lambda обрабатывает его (изменяет размер изображения, разбирает CSV, проверяет документ).
- **SNS**: сообщение публикуется в топик. Lambda получает уведомление.
- **DynamoDB Streams**: запись в DynamoDB изменяется. Lambda обрабатывает изменение.
- **CloudWatch Events (EventBridge)**: запланированное событие (например, задание cron) выполняется в заданное время.
- **ALB**: HTTP-запрос поступает на балансировщик нагрузки. Lambda может обрабатывать определённые маршруты.

В Nimbus сервис электронной почты стал Lambda-функцией, запускаемой очередью SQS. Когда в очередь поступает сообщение, Lambda вызывается с содержимым сообщения, отправляет письмо через SES (Simple Email Service) и завершает работу.

Ноль серверов. Ноль простоев. Ноль затрат при бездействии.

**Проблема холодного старта**

Lambda-функции работают в **средах выполнения** — небольших изолированных контейнерах. При вызове функции:

1. AWS проверяет, доступна ли тёплая среда выполнения (та, что обрабатывала недавний вызов)
2. Если тёплая — функция запускается немедленно
3. Если холодная — AWS инициализирует новую среду выполнения: загружает код, запускает среду выполнения, выполняет код инициализации — и затем запускает функцию

**Холодный старт** добавляет от 100 мс до нескольких секунд задержки в зависимости от среды выполнения (у Java и .NET холодный старт дольше, чем у Python и Node.js) и размера пакета с кодом.

Для асинхронной обработки (отправка писем, изменение размера изображений) холодные старты незаметны для пользователей.

Для синхронных API (HTTP-запросы, где пользователь ожидает ответа) холодные старты могут периодически вызывать медленные ответы.

**Способы смягчения**:

- **Provisioned concurrency**: предварительный прогрев заданного количества сред выполнения. Они всегда готовы к работе. Вы платите за это, даже когда они не обрабатывают запросы.
- **Уменьшение размера пакета**: меньший код инициализируется быстрее.
- **Прогревающие вызовы**: периодические «пинги» для поддержания функций в тёплом состоянии (распространённый, но неэлегантный подход).
- **Выбор подходящей среды выполнения**: Python и Node.js запускаются быстрее, чем Java.

**Ценообразование Lambda: почему Tom улыбнулся**

Ценообразование Lambda состоит из двух компонентов:

1. **Плата за запросы**: $0,20 за миллион вызовов
2. **Плата за время выполнения**: $0,0000166667 за ГБ-секунду (выделенная память × время выполнения в секундах)

Первый миллион запросов в месяц бесплатен (всегда, не только в первый год).

Tom посчитал для сервиса электронной почты:

- 1 200 писем в день × 30 дней = 36 000 вызовов в месяц
- Каждый вызов занимает ~2 секунды при 256 МБ памяти
- Время выполнения: 36 000 × 2 × 0,25 ГБ × $0,0000166667 = $0,30/мес.
- Запросы: 36 000 << 1 000 000 (бесплатный уровень) = $0,00/мес.

EC2-инстанс для сервиса электронной почты: $18/мес.

Tom помолчал немного. Потом сказал: — Нам нужно перевести на Lambda всё.

**Для чего Lambda подходит (и для чего нет)**

Lambda отлично подходит для:

- **Событийно-управляемой обработки**: реагирование на события (загрузка файлов, сообщения в очереди, плановые задачи)
- **Кратковременных задач**: обработка, завершающаяся в пределах 15 минут
- **Непредсказуемого пикового трафика**: Lambda масштабируется от 0 до тысяч мгновенно — без предварительного выделения ресурсов
- **Нерегулярных операций**: отчёт, формируемый ежедневно в 2 ночи; задание очистки, выполняемое раз в неделю
- **Вспомогательного кода**: небольшие функции, перемещающие данные между сервисами

Lambda плохо подходит для:

- **Длительных процессов**: ограничение в 15 минут — жёсткая стена
- **Приложений с состоянием**: Lambda-функции по своей природе не имеют состояния — каждый вызов независим
- **Высокопроизводительных API с малой задержкой**: холодные старты могут вызывать всплески задержки; provisioned concurrency снижает этот риск, но увеличивает стоимость
- **Приложений, требующих постоянных соединений**: Lambda не может легко поддерживать долгоживущий пул соединений с базой данных (хотя инструменты пулинга соединений, такие как RDS Proxy, помогают)
- **Традиционных веб-серверов**: возможно, но не самый подходящий вариант

— Значит, Lambda — не замена EC2, — сказала Maya. — Это другой инструмент для других задач.

— Веб-API Nimbus остаётся на EC2 или ECS, — подтвердил Leo. — Сервис электронной почты, сервис изменения размера изображений, ночной генератор отчётов, очистчик логов — всё это переедет в Lambda.

**Философия бессерверности**

Lambda — часть более широкой концепции: **бессерверности** — построения приложений, в которых вы управляете только кодом, но не серверами.

Полностью бессерверный стек Nimbus мог бы выглядеть так:

- API Gateway + Lambda (вместо EC2 с веб-сервером)
- DynamoDB (вместо RDS — тоже бессерверная, без управления сервером)
- S3 (статические ресурсы — изначально бессерверный)
- SNS + SQS (обмен сообщениями — бессерверный)
- Lambda (вся фоновая обработка)

Привлекательность: вы пишете код; AWS управляет всем остальным. Никакого обновления, никакой настройки масштабирования, никакого планирования ёмкости.

Реальность: бессерверные системы имеют собственную операционную сложность — отладка распределённых Lambda-функций, управление холодными стартами, понимание лимитов параллелизма. Это не проще, просто иначе.

## Преимущества и ограничения

**Почему Lambda мощна**:

- Истинная оплата по факту использования — нулевая стоимость при бездействии
- Автоматическое масштабирование без настройки
- Нет серверов для обновления и обслуживания
- Щедрый бесплатный уровень (1 миллион запросов в месяц, бесплатно навсегда)
- Тесная интеграция с остальными сервисами AWS

**Где возникают сложности**:

- Холодные старты реальны и требуют тщательной обработки для нагрузок, чувствительных к задержке
- Ограничение в 15 минут исключает длительные задачи
- Отладка сложнее — нет постоянного сервера для подключения по SSH
- Дизайн без состояния требует хранения всего состояния во внешних системах (база данных, кэш, S3)
- Лимиты параллелизма (по умолчанию 1 000 одновременных вызовов на аккаунт) могут ограничить производительность при масштабировании
- Lambda-функции, подключённые к VPC, имеют дополнительную задержку и проблемы с холодным стартом

## Резюме

- **AWS Lambda** запускает код в ответ на события, не управляя серверами.
- **Оплата по использованию**: тарифицируется за вызов и за каждые 1 мс выполнения (округлённые вверх). Нулевая стоимость при бездействии.
- Автоматически масштабируется от 0 до тысяч одновременных вызовов.
- **Холодные старты**: задержка инициализации при отсутствии тёплой среды выполнения. Устраняется с помощью provisioned concurrency или лёгких сред выполнения.
- Лучше всего подходит для: событийно-управляемых, кратковременных, пиковых или нерегулярных нагрузок.
- Не идеально для: длительных задач, приложений с состоянием, высокопроизводительных API с малой задержкой без provisioned concurrency.
- **Бессерверность** — философия проектирования: вы управляете кодом, а не инфраструктурой.

## Советы к экзамену

*SAA-C03, область: проектирование отказоустойчивых архитектур (область 2, задача 2.1)*

- **Lambda + S3**: классическая схема — файл, загруженный в S3, запускает Lambda для обработки (генерация миниатюр, сканирование на вирусы, преобразование данных). Сервер не нужен.
- **Lambda + SQS**: Lambda опрашивает SQS и обрабатывает пакеты. SQS предоставляет механизм повтора/DLQ. Lambda выполняет обработку.
- **Lambda + API Gateway**: бессерверный HTTP API. API Gateway управляет маршрутизацией, аутентификацией, ограничением запросов. Lambda реализует бизнес-логику.
- **Признаки холодного старта**: «всплески задержки при первом запросе», «непоследовательное время ответа» → холодный старт. Решение: provisioned concurrency (платный), меньший пакет, более лёгкая среда выполнения.
- **Лимиты выполнения**: максимум 15 минут. Максимум 10 ГБ памяти. 512 МБ эфемерного хранилища /tmp по умолчанию (настраивается до 10 ГБ). Эти ограничения встречаются в экзаменационных сценариях.
- **Параллелизм Lambda**: по умолчанию 1 000 одновременных выполнений на аккаунт (можно увеличить). **Reserved concurrency**: гарантирует функции определённое количество выполнений; не позволяет другим функциям их занять. **Provisioned concurrency**: предварительно прогревает заданное число сред выполнения.
- **Event source mapping**: функция Lambda, соединяющая SQS/DynamoDB Streams/Kinesis с Lambda. Lambda опрашивает источник и формирует пакеты записей.

## Упражнения

**Упражнение 1 — Воспроизведение**

Объясните проблему холодного старта. В каком типе приложений холодные старты наиболее критичны? В каком — приемлемы?

*(Подсказка: сравните API реального времени, где пользователь ожидает ответа, с асинхронной фоновой задачей, где пользователь уже получил подтверждение и занимается другими делами.)*

**Упражнение 2 — Подготовка к экзамену**

*Сценарий*: Компания получает от своих поставщиков изображения продуктов через корзину S3. Каждое изображение нужно изменить до четырёх стандартных размеров (миниатюра, маленький, средний, большой) и сохранить обратно в S3. Объём непредсказуем: в одни дни 10 изображений, в другие — 100 000. Обработка каждого изображения должна завершаться в течение 10 минут. Стоимость должна быть минимальной.

Какая архитектура наилучшим образом отвечает этим требованиям?

A) EC2-инстансы в Auto Scaling Group, отслеживающие корзину S3 с помощью длинного опроса  
B) Уведомление о событии S3, запускающее Lambda-функцию, которая изменяет размер изображений и сохраняет результаты в S3  
C) ECS Fargate, запускаемые очередью SQS, с событиями S3, публикуемыми в очередь  
D) Выделенный EC2-инстанс с заданием cron, каждую минуту проверяющим S3 на наличие новых изображений

**Подсказка 1**: Непредсказуемый объём отдаёт предпочтение масштабированию до нуля. Какой вариант это обеспечивает?

**Подсказка 2**: 10 минут на изображение укладываются в 15-минутный лимит Lambda. Проверьте, соответствует ли работа по изменению размера изображений ограничениям Lambda.

**Подсказка 3**: Выделенный EC2-инстанс, работающий круглосуточно, дорог и не масштабируется.

**Ответ**: B

**Пояснение**: Уведомления о событиях S3 запускают Lambda при загрузке изображения. Lambda изменяет размер изображения до четырёх вариантов и сохраняет результаты в S3. Lambda автоматически масштабируется от 0 до тысяч одновременных вызовов, справляясь с непредсказуемым объёмом без предварительного выделения ресурсов. Нулевая стоимость при отсутствии обрабатываемых изображений.

**Почему не A?** EC2 в ASG не масштабируется до нуля — минимум один инстанс всегда работает. Длинный опрос S3 — не стандартный механизм событий S3. Более высокая стоимость, чем у Lambda, для пиковых нагрузок.

**Почему не C?** ECS Fargate работает, но сложнее (требует управления контейнерами, ECR, определениями задач) и имеет несколько большую задержку холодного старта, чем Lambda, для пиковых нагрузок. Lambda проще для данного варианта использования.

**Почему не D?** Выделенный EC2-инстанс — единая точка отказа, не масштабируется, работает круглосуточно, а подход на основе cron создаёт задержку обнаружения до 60 секунд.

*SAA-C03, область: проектирование отказоустойчивых архитектур — задача 2.1*

**Упражнение 3 — Архитектурный вызов** *(Дополнительно)*

Nimbus хочет ежедневно в 5:00 генерировать отчёт с топ-10 ресторанов по объёму заказов за предыдущий день. Отчёт формируется из данных DynamoDB, форматируется как PDF, сохраняется в S3 и рассылается всем партнёрам-ресторанам по электронной почте.

Разработайте полный конвейер на основе Lambda для этой задачи. Что запускает Lambda? Что произойдёт, если генерация PDF займёт 12 минут? Что если партнёров-ресторанов 5 000 и рассылка им письм занимает время? Использовали бы вы одну Lambda или несколько?

*(Единственно правильного ответа не существует. Цель — практика построения Lambda с другими сервисами.)*

## Сцена после титров

Tom просмотрел счёт в конце месяца.

Сервис электронной почты: исчез из счёта EC2.
Задание по изменению размера изображений: исчезло.
Ночная задача очистки: исчезла.
Ежедневный аналитический отчёт: исчез.

Итоговые начисления Lambda за месяц: $4,23.

— Четыре доллара, — сказал Tom.

— И двадцать три цента, — услужливо добавил Leo.

Tom посмотрел на счёт за предыдущий месяц, когда все эти сервисы работали на EC2-инстансах.

— Мы платили $187 за те же нагрузки.

— Lambda не берёт плату за простой, — сказал Leo. — А большинство этих сервисов простаивали 90% времени.

Tom долго смотрел на экран.

— Беру обратно всё, что говорил о бессерверности как о модном слове, — произнёс он.

В следующей главе: контейнер для доставки, который заставляет любой сервер чувствовать себя как дома.
