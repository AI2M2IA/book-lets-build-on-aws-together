# Глава 17: Наблюдатели

Инцидент с румынским IP был ограничен. Секреты были в Secrets Manager. Учётные данные ротировались. Сетевые контроли были ужесточены.

Но Прия задала вопрос, которым закончилась Глава 16: «Если бы что-то необычное появилось в CloudTrail, как бы мы узнали?»

Честный ответ был: вероятно, нет.

CloudTrail логирует тысячи событий в день. Ни один человек их не читает все. Прия проверяла вручную каждую неделю, но это означало, что что-то могло произойти во вторник и остаться незамеченным до следующего понедельника.

«Нам нужно что-то, что следит за логами вместо нас», — сказала она.

Майя подняла глаза. «Автоматически?»

«Автоматически».

Второй вопрос Тома за день: «Сколько это стоит?»

**Три категории угроз**

Угрозы безопасности против облачного приложения в целом делятся на три категории:

**Объёмные атаки (DDoS)**: Злоумышленник отправляет столько трафика, что ваше приложение не может отвечать законным пользователям. Атака может быть миллионами HTTP-запросов или потоком TCP SYN-пакетов, спроектированных для исчерпания таблицы соединений вашего сервера.

**Атаки на приложение (Эксплойты)**: Злоумышленник отправляет специально созданные запросы, спроектированные для эксплуатации слабостей вашего приложения — SQL-инъекции, межсайтовый скриптинг, неправильный ввод, вызывающий сбой парсера.

**Поведенческие аномалии (Разведка и компрометация)**: API-вызовы, которых не должно быть (кто-то запрашивает всю базу пользователей в 3 часа ночи), необычная IAM-активность (учётные данные используются из новой страны) или сетевой трафик к неожиданным назначениям.

AWS имеет специализированный сервис для каждого:

- **AWS Shield**: Защита от DDoS
- **AWS WAF**: Защита на уровне приложения
- **Amazon GuardDuty**: Обнаружение поведенческих угроз

**AWS Shield: поглотитель DDoS**

**AWS Shield Standard** автоматически включён для всех клиентов AWS без дополнительных затрат. Он защищает от наиболее распространённых атак DDoS уровней 3 (сеть) и 4 (транспорт) — SYN-флуды, UDP-флуды, атаки DNS-амплификации.

CloudFront, Route 53 и Elastic Load Balancing находятся на границе сети AWS. Когда DDoS-атака нацелена на ваше приложение, она сначала ударяет в эти управляемые сервисы. Сетевая инфраструктура AWS поглощает атаку до того, как она достигает ваших экземпляров EC2.

**AWS Shield Advanced** — это премиум-уровень ($3 000/месяц на организацию). Он добавляет:

- Защиту для EC2, ELB, CloudFront, Global Accelerator и Route 53
- Оповещения об атаках почти в реальном времени
- Доступ к AWS Shield Response Team (SRT) — инженеры безопасности, которые могут помочь вам реагировать на атаки
- Защиту от затрат: если атака вызывает скачок вашего счёта, AWS возмещает дополнительные расходы
- Расширенное обнаружение DDoS и смягчение последствий на уровне 7 (уровень приложения)

«Три тысячи долларов в месяц?» — сказал Том.

«Для предприятий, обрабатывающих миллионы в выручке, DDoS, отключающий их на два часа, стоит больше трёх тысяч долларов», — сказала Прия.

Том посчитал в уме.

«Начнём со Стандарта», — сказал он наконец.

**AWS WAF: фильтр приложений**

**AWS WAF (Web Application Firewall)** работает на HTTP-уровне — он инспектирует содержимое веб-запросов до того, как они достигают вашего приложения.

WAF настраивается с **Web ACL (Access Control Lists)** — наборами правил, определяющими, что разрешить, заблокировать или посчитать.

WAF можно прикрепить к:

- Дистрибутивам CloudFront (инспектировать запросы на граничном уровне, глобально)
- Application Load Balancers (инспектировать запросы на региональном уровне)
- API Gateway
- AWS AppSync

**Управляемые правила WAF**: AWS и сторонние поставщики публикуют готовые наборы правил:

- **AWS Managed Rules - Core Rule Set**: Защита от уязвимостей OWASP Top 10 (SQL-инъекция, XSS, инъекция команд, обход пути и т.д.)
- **AWS Managed Rules - Known Bad Inputs**: Блокирует запросы, совпадающие с известными паттернами атак
- **AWS Managed Rules - Amazon IP Reputation List**: Блокирует IP, известные связью с ботнетами и сканерами
- **AWS Managed Rules - Bot Control**: Идентифицирует и управляет трафиком ботов

Вы также можете создавать пользовательские правила:

- «Блокировать любой запрос с заголовком User-Agent, содержащим 'sqlmap'» (распространённый сканер SQL-инъекций)
- «Ограничение скорости: не более 1000 запросов с одного IP за 5 минут»
- «Блокировать запросы, содержащие `<script>` в любом значении параметра»

Для Nimbus практическая настройка: WAF на дистрибутиве CloudFront с включённым Core Rule Set. Это блокирует наиболее распространённые паттерны атак до того, как запросы когда-либо достигают экземпляров EC2.

**Amazon GuardDuty: поведенческий аналитик**

GuardDuty фундаментально отличается от Shield и WAF. Он не блокирует атаки — он **обнаруживает необычное поведение**.

GuardDuty непрерывно анализирует:

- **Логи AWS CloudTrail**: Изменения IAM, API-вызовы, входы в консоль
- **Логи потоков VPC**: Паттерны сетевого трафика в вашем VPC
- **Логи DNS-запросов**: Что ваши экземпляры разрешают (известные вредоносные программы часто разрешают конкретные домены C2)

Модели машинного обучения идентифицируют паттерны, отклоняющиеся от вашей базовой линии. GuardDuty генерирует **обнаружения** — категоризированные оповещения — при обнаружении аномалий.

Примеры того, что может обнаружить GuardDuty:

- IAM-пользователь, входящий с неизвестного IP-адреса (в стране, которую он никогда не использовал)
- API-вызовы, сделанные с Tor exit-узла
- Экземпляр EC2, общающийся с известным пулом майнинга криптовалюты
- Необычно высокий объём API-вызовов (злоупотребление учётными данными или сканирование)
- Доступ к бакету S3 с IP-адреса, помеченного как вредоносный
- Исходящий трафик к домену, известному связью с вредоносным программным обеспечением командования и контроля

«Это то, что бы перехватило румынский IP», — тихо сказал Лео.

«Если бы у нас был включён GuardDuty, он бы пометил экземпляр EC2, делающий исходящие соединения к неизвестному внешнему IP в 2 часа ночи», — подтвердила Прия.

«Сколько это стоит?»

Цены GuardDuty основаны на объёме анализируемых логов — события CloudTrail, данные потоков VPC, DNS-запросы. Для небольшого или среднего приложения обычно $50-150/месяц. В масштабе это всё равно небольшая часть затрат на инфраструктуру.

Том открыл консоль и включил его.

**Связывание трёх сервисов**

Shield, WAF и GuardDuty работают на разных уровнях и дополняют друг друга:

| Сервис     | Уровень                   | Защищает от                              | Действие                              |
|------------|---------------------------|------------------------------------------|---------------------------------------|
| AWS Shield | Сеть/Транспорт (L3/L4)    | DDoS-флуды                               | Поглощает/смягчает атаки              |
| AWS WAF    | Приложение (L7)           | OWASP Top 10, боты, скрейперы            | Разрешает, блокирует или считает запросы |
| GuardDuty  | Поведенческий (все логи)  | Аномалии, скомпрометированные учётные данные, вредоносные программы | Обнаруживает и оповещает |

Shield останавливает поток. WAF фильтрует воду. GuardDuty следит за сантехникой на предмет необычных паттернов потока.

**CloudTrail: фундамент**

Все три сервиса полагаются на логи. **AWS CloudTrail** — это сервис логирования, фиксирующий каждый API-вызов в вашем аккаунте AWS — кто что вызвал, когда, откуда, с каким результатом.

CloudTrail включён по умолчанию для 90-дневной истории в консоли. Для долгосрочного хранения логов:

1. Создайте trail, записывающий в бакет S3
2. Опционально отправляйте в CloudWatch Logs для оповещений в реальном времени
3. Включите валидацию файла логов (чтобы обнаруживать вмешательство в логи)

GuardDuty, AWS Config и Security Hub читают из CloudTrail. Без логов CloudTrail этим сервисам нечего анализировать.

**AWS Security Hub: дашборд**

Если вы управляете несколькими аккаунтами AWS или вам нужен консолидированный вид обнаружений безопасности, **AWS Security Hub** агрегирует обнаружения от GuardDuty, Inspector (оценка уязвимостей), Macie (конфиденциальность данных), Config и Firewall Manager в единый дашборд.

Он также проверяет вашу конфигурацию на соответствие лучшим практикам безопасности (стандарт AWS Foundational Security Best Practices) и CIS AWS Foundations Benchmark.

Для Nimbus: Security Hub пока не был нужен. Когда они выросли до трёх аккаунтов (разработка, тестирование, производство), он стал бы полезным.

## Сильные стороны и ограничения

**AWS Shield**:

- Standard: бесплатный и автоматический — нет причин не использовать
- Advanced: отлично для громких целей; дорого для небольших команд

**AWS WAF**:

- Управляемые группы правил значительно упрощают настройку
- Пользовательские правила требуют понимания паттернов атак HTTP
- Ограничение скорости — мощная функция, которую часто упускают из виду
- WAF не заменяет безопасный код приложения — это уровень эшелонированной защиты

**GuardDuty**:

- Крайне мало усилий для включения (несколько кликов)
- Обнаружения требуют проверки и реагирования человеком — GuardDuty обнаруживает, он не исправляет
- Ложные срабатывания случаются — некоторая законная активность выглядит аномальной для ML-моделей
- 30-дневный бесплатный период — стоит включить немедленно

## Итоги

- **AWS Shield Standard**: Бесплатная, автоматическая DDoS-защита на уровнях 3/4. Всегда включена.
- **AWS Shield Advanced**: Премиум DDoS-защита с доступом к SRT и защитой от затрат. Корпоративный случай использования.
- **AWS WAF**: Файрвол уровня приложения. Инспектировать и фильтровать HTTP-запросы. Прикрепить к CloudFront, ALB или API Gateway. Использовать Managed Rule Groups для защиты OWASP Top 10.
- **Amazon GuardDuty**: Обнаружение поведенческих угроз. Анализирует CloudTrail, VPC Flow Logs и DNS-логи. Генерирует обнаружения для аномальной активности.
- **CloudTrail**: Фундамент всего логирования безопасности AWS. Включите trail, записывающий в S3 для долгосрочного хранения.
- Эти сервисы дополняют друг друга: Shield на сетевом уровне, WAF на уровне приложения, GuardDuty на поведенческом уровне.

## Советы по экзамену

*Домен SAA-C03: Проектирование безопасных архитектур (Домен 1, Задача 1.2)*

- **Shield Standard против Advanced**: Standard бесплатный и автоматический. Advanced стоит денег и добавляет SRT, защиту от затрат и лучшее обнаружение. Сигналы экзамена для Advanced: «крупномасштабный DDoS», «гарантия SLA при атаках», «финансовая защита от скачков затрат, связанных с DDoS».
- **Сигналы использования WAF**: «блокировать SQL-инъекцию», «блокировать межсайтовый скриптинг», «ограничение скорости API-вызовов», «блокировать конкретные user-agents», «защита OWASP Top 10» → WAF.
- **Сигналы GuardDuty**: «обнаруживать необычную API-активность», «идентифицировать скомпрометированные учётные данные», «помечать аномальные сетевые соединения EC2», «разведка угроз» → GuardDuty.
- **Прикрепление WAF**: Может прикрепляться к CloudFront (глобально), ALB (регионально), API Gateway (регионально), AppSync.
- **Источники данных GuardDuty**: События управления CloudTrail, события данных S3 CloudTrail, VPC Flow Logs, DNS-логи. Экзамен может спрашивать, какой источник данных релевантен для конкретного сценария обнаружения.
- **Macie**: Часто путают с GuardDuty. **Macie** использует ML для обнаружения конфиденциальных данных в S3 (персональные данные, учётные данные, финансовые данные). **GuardDuty** обнаруживает угрозы и аномалии в поведении. Разные случаи использования.

## Упражнения

**Упражнение 1 — Запоминание**

Объясните разницу между AWS WAF и Amazon GuardDuty. От чего защищает каждый сервис и на каком уровне каждый работает?

*(Подсказка: думайте о WAF как о фильтре на входящих запросах, а о GuardDuty как о поведенческом аналитике, следящем за вашими логами.)*

**Упражнение 2 — Практика для экзамена**

*Сценарий*: Веб-сайт розничной компании подвергается атаке ботнета, отправляющего миллионы запросов в час к их API поиска по товарам. Запросы выглядят законными (правильные строки User-Agent, действительные куки сессии), но не приводят к покупкам — они парсят цены товаров. Атака вызывает медленное время отклика для законных клиентов.

Какая комбинация сервисов НАИЛУЧШИМ образом решает эту угрозу?

А) AWS Shield Advanced и CloudFront  
Б) AWS WAF с правилами ограничения скорости и CloudFront  
В) Amazon GuardDuty и AWS Shield Standard  
Г) Сетевые ACL, блокирующие IP-диапазоны ботнета

**Подсказка 1**: Запросы находятся на HTTP-уровне (уровень приложения). Какой сервис работает на HTTP-уровне?

**Подсказка 2**: Ботнеты используют много разных IP-адресов — блокировка конкретных IP-диапазонов на уровне NACL неэффективна против крупных ботнетов.

**Подсказка 3**: Ограничение скорости по IP-адресу может замедлить парсинг, даже если его нельзя полностью заблокировать.

**Ответ**: Б

**Объяснение**: AWS WAF может ограничивать скорость запросов на IP-адрес, снижая воздействие высокообъёмного парсинга с любого одного источника. CloudFront распределяет входящий трафик по граничной сети AWS, поглощая объём и защищая источник. Правила WAF также могут соответствовать паттернам запросов (быстрые последовательные запросы к одной и той же API-конечной точке) для идентификации поведения парсинга.

**Почему не А?** Shield Advanced защищает от DDoS-флудов (уровни 3/4). Сценарий описывает парсинг на уровне приложения (HTTP-запросы уровня 7), который Shield не инспектирует.

**Почему не В?** GuardDuty обнаруживает аномалии в поведении вашего аккаунта AWS — он не блокирует входящие HTTP-запросы. Shield Standard не обрабатывает атаки уровня приложения.

**Почему не Г?** Крупные ботнеты используют тысячи IP-адресов из распределённых источников. Блокировка конкретных диапазонов — это подход «ударь крота», который не работает против сложных ботнетов.

*Домен SAA-C03: Проектирование безопасных архитектур — Задача 1.2*

**Упражнение 3 — Архитектурный вызов** *(Необязательно)*

Nimbus готовится к обработке данных кредитных карт. Проверка соответствия PCI-DSS требует:

- Защита от DDoS-атак на сетевом уровне
- Фильтрация на уровне приложения от известных веб-эксплойтов
- Логирование всех API-вызовов в защищённое от вмешательства долгосрочное хранилище
- Обнаружение необычных паттернов доступа к платёжному сервису

Сопоставьте каждое требование с конкретным сервисом AWS или конфигурацией. Достаточен ли Shield Standard, или контекст PCI-DSS подразумевает Advanced? Где вы бы прикрепили WAF?

*(Нет единственно правильного ответа. Цель — практиковать сопоставление требований соответствия с сервисами AWS.)*

## Послесловие к главе

GuardDuty был включён.

Через сорок восемь часов он сгенерировал своё первое обнаружение: *«Экземпляр EC2 i-0abc123 общается с известным Tor exit-узлом».*

Лео посмотрел на ID экземпляра.

«Это внутренний экземпляр мониторинга», — сказал он. «Тот, который я настроил для запуска сетевой диагностики».

«Ему предположительно общаться с Tor exit-узлами?»

«Нет». Он сделал паузу. «Почему бы ему?»

Он открыл экземпляр. Кто-то установил на него инструмент — законный инструмент с открытым исходным кодом для сетевого сканирования, который, как выяснилось, также общался с инфраструктурой Tor для анонимизированного сбора данных.

«Значит инструмент звонил домой», — сказала Прия.

«Без моего ведома», — подтвердил Лео.

«Это риск цепочки поставок. Зависимость, делающая вещи, которые вы не авторизовывали».

Лео удалил инструмент. Он установил процесс проверки каждого стороннего инструмента перед установкой.

«Это уровень паранойи, которого мы теперь достигли?» — спросила Майя.

«Да», — сказала Прия.

«Это уровень, которого мы должны были всегда достигать?» — спросила Майя.

«Тоже да», — сказала Прия.

В следующей главе: что происходит, когда дата-центр в Вирджинии исчезает — и почему Nimbus продолжает работать.
