# الفصل التاسع: حين يكبر الجدول

كان في جدول القائمة خمسون ألف صنف.

موزّعة على 287 مطعماً، لكل منها عروض يومية وأصناف موسمية وتنويعات إقليمية. بعض الأصناف تحتوي على معدّلات — الحجم، مستوى الحرارة، اختيار البروتين. وبعضها صفقات مجمّعة تُحيل إلى أصناف أخرى. وبعضها يظهر في القائمة أيام الأسبوع فقط، أو وقت الغداء فقط، أو في مدن بعينها.

كان استعلام SQL الذي يسترجع قائمة المطعم الكاملة يُكمل في 200 مللي ثانية.

الآن استغرق أربع ثوانٍ.

أربع ثوانٍ هي الفارق بين شخص يُسجّل طلباً وشخص يُغلق التطبيق. أجرى Leo تحليل خطة الاستعلام. نظر Tom في ضبط الفهرس. زادت Priya عدد نسخ القراءة. لم يُفضِ شيء من ذلك إلى فارق ملموس.

وهذا غيّر الأجواء في الغرفة.

حين تصمد مشكلة أمام الفهرسة ومحاولات التخزين المؤقت ونسخة متماثلة إضافية، يتوقف الناس عن افتراض أن الحل سيكون ذكياً.

أحياناً يكون الحل أن شكل النظام خاطئ.

قال Leo: "المشكلة في شكل البيانات. SQL يريد كل شيء في صفوف وأعمدة. قوائمنا ليس لها شكل ثابت."

كان هذا بداية محادثة أطول.

**مشكلة احتواء كل شيء في جدول**

هنا يكمن التوتر الجوهري في قواعد البيانات العلائقية: صُمّمت لتخزين بيانات *منظّمة* في أشكال *ثابتة*.

لو كان لكل صنف في القائمة نفس الحقول — الاسم، السعر، الوصف، الفئة — لكان SQL مثالياً. سيكون لديك جدول `menu_items` نظيف، وصفوف لكل صنف، واستعلامات منطقية.

لكن القوائم الحقيقية لا تعمل هكذا.

قد يحتوي صنف ما على معدّل "مستوى الحرارة". وصنف آخر على "اختيار البروتين". وصنف ثالث على مجموعات متداخلة — "اطلب وجبة العائلة وستحصل على طبقين رئيسيين وطبقين جانبيين ومشروباً." شكل البيانات يتغير *لكل صنف*.

في SQL، أمامك خيارات:

**الخيار الأول**: إنشاء عمود لكل معدّل محتمل. هذا ينتج جدولاً عريضاً جداً حيث معظم الأعمدة فارغة في معظم الأوقات.

**الخيار الثاني**: إنشاء جدول معدّلات منفصل وربطه بجدول أصناف القائمة. يصلح هذا، لكن القوائم المعقدة تتطلب ضمّات متعددة، وعند خمسين ألف صنف مع حجم قراءة مرتفع، تصبح هذه الضمّات مكلفة.

قالت Priya التي كانت تقرأ الوثائق بهدوء في الزاوية: "هناك خيار ثالث."

رفعت نظرها. "ماذا لو لم تكن البيانات مضطرة للاندراج في جدول؟"

**طريقة مختلفة للتفكير في البيانات**

تخزّن قواعد البيانات العلائقية البيانات كصفوف في جداول. يجب أن يتوافق كل صف مع مخطط الجدول. يُتفق على المخطط مسبقاً.

تخزّن قواعد بيانات NoSQL البيانات بطريقة مختلفة. أحد الأساليب الشائعة هو *نموذج المستند*: يُخزَّن كل سجل كمستند مستقل (عادةً JSON)، ولا يلزم أن تحتوي المستندات في نفس المجموعة على نفس الحقول.

قد يبدو صنف في القائمة بنموذج المستند هكذا:

```json
{
  "itemId": "ITEM-001",
  "restaurantId": "NIMBUS-047",
  "name": "Shrimp Arepa",
  "price": 3200,
  "modifiers": [
    { "name": "Spice Level", "options": ["mild", "medium", "hot"] },
    { "name": "Protein", "options": ["shrimp", "fish", "mixed"] }
  ],
  "available": true,
  "seasonalUntil": "2024-03-31"
}
```

وقد يبدو صنف آخر مختلفاً تماماً:

```json
{
  "itemId": "ITEM-002",
  "restaurantId": "NIMBUS-047",
  "name": "Family Feast",
  "price": 9800,
  "includes": ["ITEM-010", "ITEM-011", "ITEM-015", "ITEM-020"],
  "servings": 4,
  "available": true
}
```

أشكال مختلفة. نفس المجموعة. لا مشكلة.

قالت Maya: "إذن قاعدة البيانات أشبه بنظام ملفات أكثر من كونها جدولاً."

قالت Priya: "بالضبط. يمكنك وضع أي مستند في أي درج. لا تحتاج إلى قصّ المستند ليناسب حجماً ثابتاً."

**التعرف على DynamoDB**

Amazon DynamoDB هي خدمة قواعد بيانات NoSQL المُدارة من AWS. تخزّن البيانات كعناصر (وليس صفوفاً)، وتُجمَع العناصر في جداول (التسمية مشابهة لـ SQL، لكن السلوك مختلف).

يجب أن يحتوي كل عنصر في جدول DynamoDB على **مفتاح أساسي** يُعرّفه بشكل فريد. كل شيء آخر مرن.

يمكن أن يكون المفتاح الأساسي بأحد شكلين:

**مفتاح التقسيم فقط**: سمة واحدة يجب أن تكون فريدة عبر جميع العناصر.

**مفتاح التقسيم + مفتاح الترتيب (مفتاح أساسي مركّب)**: سمتان *تشكّلان معاً* مجموعة فريدة. هذا يتيح وجود عناصر متعددة بنفس مفتاح التقسيم، تتميّز بمفاتيح ترتيبها.

بالنسبة لقائمة Nimbus:

- مفتاح التقسيم: `restaurantId`
- مفتاح الترتيب: `itemId`

هذا يعني أنك تستطيع استرجاع جميع الأصناف لمطعم محدد بكفاءة — DynamoDB يعرف بالضبط أي قسم يبحث فيه.

سأل Tom: "لماذا يسمى مفتاح تقسيم؟"

**كيف يخزّن DynamoDB البيانات داخلياً**

بُني DynamoDB ليتوسع أفقياً لأحجام هائلة. يُحقق هذا من خلال *التقسيم* — تُوزَّع البيانات عبر العديد من الأجهزة الفيزيائية بناءً على مفتاح التقسيم.

حين تكتب عنصراً، تُجري DynamoDB تحويلاً هاش لقيمة مفتاح التقسيم وتستخدم ذلك الهاش لتحديد القسم الفيزيائي (وبالتالي الخادم) الذي يخزّن العنصر. حين تقرأ عنصراً، تُجري DynamoDB نفس الحساب لإيجاده على الفور.

فكّر في الأمر كنظام بريد. إذا كان لكل مغلف رمز بريدي، لا يحتاج مكتب البريد إلى قراءة كل مغلف لمعرفة وجهته — يُفرز بحسب الرمز البريدي. DynamoDB تُفرز بحسب هاش مفتاح التقسيم.

لهذا يهم اختيار مفتاح تقسيم جيد:

- **جيد**: قيم عالية الكثافة، موزّعة بالتساوي (`restaurantId` مع العديد من المطاعم)
- **سيئ**: قيم منخفضة الكثافة (`true/false`، `category`) — تستقر معظم البيانات في أقسام قليلة، مما يخلق "نقاطاً ساخنة"

النقطة الساخنة تعني أن قسماً واحداً يستقبل معظم الزيارات. يصبح ذلك القسم عنق الزجاجة. تبدأ DynamoDB في تقييد الطلبات. يبدأ المستخدمون في رؤية الأخطاء.

قال Leo ببطء: "إذن لو استخدمت `available: true` كمفتاح تقسيم، ستتكدس جميع الأصناف المتاحة في نفس القسم."

أكدت Priya: "وستنهار قاعدة بياناتك في وقت ذروة العشاء."

أغلق Leo حاسوبه المحمول ببطء.

**القراءة والكتابة على نطاق واسع**

DynamoDB قادرة على التعامل مع ملايين الطلبات في الثانية. لكنها تحتاج إلى معرفة مقدار السعة الواجب تخصيصها.

هناك وضعان للسعة:

**السعة المخصصة مسبقاً**: تُحدد عدد وحدات القراءة والكتابة التي تريدها. تحجز DynamoDB تلك السعة لك وتُقيّد الزيارات التي تتجاوزها. تكلفة قابلة للتنبؤ، وسعر أقل لكل طلب.

**السعة عند الطلب**: تتوسع DynamoDB تلقائياً مع حجم زياراتك الفعلية. لا حاجة لتخطيط سعة دورية. تكلفة أعلى لكل طلب، وأبسط تشغيلياً بكثير، رغم أن الارتفاعات المفاجئة التي تتجاوز بكثير نمط الزيارات الأخير للجدول قد تُسبب تقييداً إذا تصاعدت بسرعة كبيرة.

بالنسبة لـ Nimbus، تُقرأ القائمة أكثر بكثير مما تُكتب. يفتح العميل التطبيق، يتصفح القائمة — هذه قراءات كثيرة. يُحدّث شريك المطعم قائمته مرتين في الأسبوع — هذه كتابات متفرقة.

قال Tom: "السعة عند الطلب منطقية الآن. لا نعرف بعد أنماط الزيارات لدينا. أفضل أن ندفع أكثر لكل طلب من أن نُقيَّد بسبب نقص التخصيص."

حكمة بنية تحتية متردّدة. من Tom. نضج الفريق رسمياً.

**الاتساق: كم البيانات طازجة؟**

تُكرّر DynamoDB البيانات عبر مناطق توافر متعددة تلقائياً. هذا رائع للمتانة، لكنه يعني أنك تحتاج إلى التفكير بوضوح في اتساق القراءة.

حين تقرأ من DynamoDB، أمامك خيار:

**قراءة متسقة في نهاية المطاف**: هذا هو الإعداد الافتراضي. أرخص، وقد يتأخر الناتج قليلاً خلف كتابة أُكملت مؤخراً.

**قراءة متسقة تماماً**: لعمليات القراءة على جدول أو فهرس ثانوي محلي، يمكن لـ DynamoDB إرجاع أحدث قيمة مُكتَّبة من الكتابات الناجحة السابقة. يكلف سعة قراءة أكثر وغير متاح للفهارس الثانوية العالمية.

لبيانات القائمة، الاتساق في نهاية المطاف كافٍ. صنف في القائمة متأخر مللي ثانية لا يهم.

لبيانات تأكيد الطلب — "هل جرى تسجيل هذا الطلب؟" — ستريد اتساقاً قوياً. لا ينبغي أن يرى العميل رسالة "حاول مرة أخرى" حين كان طلبه قد حُفظ للتو.

قالت Maya: "الأمر مثل الفرق بين التحقق من رصيدك المصرفي عبر التطبيق مقابل الاتصال بالبنك مباشرةً. التطبيق قد يتأخر ثلاثين ثانية. المكالمة الهاتفية دائماً آنية."

**المفاضلة: ما لا تستطيع DynamoDB فعله**

NoSQL ليست أفضل بالضرورة من SQL. إنها أداة مختلفة لعمل مختلف.

ما تتخلى عنه DynamoDB:

**استعلامات مرنة**: في SQL، يمكنك الترشيح والترتيب بحسب أي عمود. في DynamoDB، يمكنك الاستعلام بكفاءة بحسب المفتاح الأساسي فقط. الاستعلام بحقول عشوائية يتطلب *فحصاً* (قراءة كل عنصر في الجدول)، وهو مكلف وبطيء على نطاق واسع.

**الضمّات**: لا تُجري DynamoDB ضمّات. إذا احتجت بيانات من جدولين، تُجري قراءتين منفصلتين في كود التطبيق.

**المعاملات**: تدعم DynamoDB المعاملات، لكن قواعد البيانات العلائقية لا تزال تناسب بشكل طبيعي أكثر أسير العمل المتعددة الكيانات والأنظمة الكثيفة بإعداد التقارير والتصاميم الكثيفة بالضمّات.

**الألفة**: عقود من أدوات SQL ومهاراتها ونماذجها الذهنية لا تنتقل مباشرةً.

ما تتميز فيه DynamoDB:

- أنماط الوصول للمفاتيح والمستندات
- النطاق الهائل (زمن استجابة بأرقام أحادية بالمللي ثانية بأي حجم)
- بلا خادم، لا إدارة بنية تحتية
- توسع تلقائي، نسخ متعدد مناطق التوافر، نسخ احتياطية
- أداء قابل للتنبؤ بغض النظر عن حجم البيانات

قالت Maya: "إذن القاعدة هي: استخدم DynamoDB حين تعرف *بالضبط* كيف ستصل إلى البيانات. استخدم SQL حين لا تعرف بعد."

أومأت Priya: "صمّم أنماط وصولك أولاً. ثم اختر قاعدة بياناتك."

هذا من أكثر الأشياء نضجاً التي يمكن أن تُنتجها محادثة قواعد بيانات.

**متى تستخدم كل خيار**

| الحالة | الخيار المناسب |
|--------|----------------|
| بيانات منظّمة، استعلامات معقدة، تقارير | RDS (PostgreSQL, MySQL) |
| أشكال بيانات مرنة، وصول قائم على المفاتيح، نطاق هائل | DynamoDB |
| كتابة ثقيلة مع علاقات معقدة | RDS |
| قراءة ثقيلة مع أنماط وصول قابلة للتنبؤ | DynamoDB |
| تحتاج إلى ضمّات وتجميعات | RDS |
| تحتاج إلى زمن استجابة بالمللي ثانية عند ملايين الطلبات/الثانية | DynamoDB |
| معاملات عبر كيانات متعددة | RDS (عادةً) |
| بلا خادم / ارتفاعات حركة غير متوقعة | DynamoDB عند الطلب |

الإجابة الخاطئة دائماً هي "استخدم أحدهما دائماً." انتهى الأمر بـ Nimbus إلى استخدام كليهما: RDS لسجل الطلبات والسجلات المالية (منظّمة، علائقية، تحتاج إلى تقارير)، وDynamoDB للقائمة (مخطط مرن، حجم قراءة مرتفع، وصول بحسب معرف المطعم).

## نقاط القوة والقيود

**لماذا تتميز DynamoDB**:

- زمن استجابة بأرقام أحادية بالمللي ثانية بأي حجم
- مُدارة بالكامل — لا تصحيحات، لا إعداد نسخ متماثلة، لا نوافذ صيانة
- نسخ تلقائي عبر مناطق توافر متعددة (المتانة مدمجة)
- التوسع عند الطلب يعني صفر تخطيط للسعة
- تكامل أصلي مع Lambda وAPI Gateway وStreams
- الاسترداد عند نقطة زمنية محددة (مشابه للنسخ الاحتياطية التلقائية لـ RDS)
- DynamoDB Streams — التقاط كل تغيير كحدث (مفيد للمعالجة الفورية)

**حيث يتعقّد الأمر مع DynamoDB**:

- تصميم نمط الوصول غير قابل للتفاوض — الأخطاء مكلفة للتراجع عنها
- الاستعلامات المعقدة تتطلب فهارس ثانوية (تزيد التكلفة والتعقيد)
- الفحوصات مكلفة — تجنّبها في بيئة الإنتاج
- "حد حجم العنصر" 400 كيلوبايت — العناصر الكبيرة تحتاج إلى تخزين مختلف
- قد تفاجئك الأسعار إذا لم تفهم تكاليف وحدات القراءة/الكتابة

## الملخص

- DynamoDB هي خدمة قواعد بيانات NoSQL المُدارة من AWS.
- تُخزَّن العناصر كمستندات مرنة — لا يلزم مخطط ثابت.
- يجب أن يحتوي كل عنصر على **مفتاح أساسي**: مفتاح تقسيم وحده، أو مفتاح تقسيم + مفتاح ترتيب.
- يُحدد مفتاح التقسيم القسم الفيزيائي الذي يخزّن العنصر. اختره لتوزيع متساوٍ.
- السعة **عند الطلب** تتوسع تلقائياً؛ السعة **المخصصة مسبقاً** أرخص إذا كانت حركة زياراتك قابلة للتنبؤ.
- قراءات **متسقة في نهاية المطاف** أرخص وأسرع. القراءات **المتسقة تماماً** دائماً آنية.
- تتميز DynamoDB في الوصول القائم على المفاتيح على نطاق هائل. تكافح مع الاستعلامات المخصصة والضمّات.
- استخدم RDS للبيانات العلائقية. استخدم DynamoDB لبيانات المستند/القيمة-المفتاح. استخدم كليهما حين تستدعي الحالة ذلك.

## نصائح الاختبار

*SAA-C03 المجال: تصميم معماريات عالية الأداء (المجال الثالث، المهمة 3.3)*

- اعرف قواعد مفتاح التقسيم: **كثافة عالية، توزيع متساوٍ**. الأقسام الساخنة فخ شائع في الاختبار.
- **عند الطلب مقابل المخصصة مسبقاً**: عند الطلب للزيارات غير المتوقعة؛ المخصصة مسبقاً (مع التوسع التلقائي) لأعباء العمل القابلة للتنبؤ.
- **DynamoDB Streams**: يلتقط التغييرات على مستوى العناصر في الوقت الفعلي. سيناريو اختبار شائع: "تشغيل دالة Lambda حين يتغير سجل."
- **الجداول العالمية**: نسخ متعدد المناطق ومتعدد النشاط للتطبيقات الموزّعة عالمياً وسيناريوهات الاسترداد من الكوارث. في الاختبار، هذه إشارة قوية حين يحتاج عبء العمل إلى قراءات وكتابات محلية في أكثر من منطقة.
- **DAX (DynamoDB Accelerator)**: طبقة تخزين مؤقت في الذاكرة لـ DynamoDB. يُقلل زمن استجابة القراءة من المللي ثانية إلى الميكرو ثانية. يستخدم الاختبار هذا حين لا تساعد نسخ القراءة في RDS (لأنه تخزين مؤقت خاص بـ DynamoDB).
- **المفتاح الأساسي المركّب**: مفتاح التقسيم + مفتاح الترتيب يتيح استعلامات مرنة داخل قسم. مثال: استرجاع جميع طلبات عميل بين تاريخين — `customerId` هو مفتاح التقسيم، `orderDate` هو مفتاح الترتيب.
- اعرف متى لا تستخدم DynamoDB: ضمّات معقدة، تقارير مخصصة، معاملات متعددة الكيانات ← RDS عادةً الإجابة.

## تمارين

**تمرين 1 — استرجاع**

اشرح الفرق بين مفتاح التقسيم ومفتاح الترتيب. متى ستستخدم كليهما؟

*(تلميح: فكّر في قائمة Nimbus — لماذا يجعل وجود restaurantId كمفتاح تقسيم وitemId كمفتاح ترتيب استرجاع القائمة الكاملة للمطعم أمراً فعّالاً؟)*

**تمرين 2 — تدريب على الاختبار**

*سيناريو*: تخزّن شركة ألعاب عالمية ملفات تعريف اللاعبين في DynamoDB. يتضمن كل ملف تعريف حقولاً مثل اسم المستخدم والمستوى والإنجازات والمخزون. بعض اللاعبين لديهم 10 عناصر في المخزون؛ وآخرون لديهم 5000 تكوين مخصص. تحتاج الشركة إلى زمن استجابة قراءة بأرقام أحادية بالمللي ثانية لاستعلامات الملف الشخصي أثناء اللعب النشط.

أي نهج تصميم يدعم هذا المتطلب على أفضل وجه؟

أ) الانتقال إلى RDS Aurora مع نسخ قراءة في كل منطقة  
ب) استخدام DynamoDB مع `playerId` كمفتاح تقسيم وتخزين الملف الشخصي بالكامل كعنصر واحد  
ج) استخدام DynamoDB مع `level` كمفتاح تقسيم لتجميع اللاعبين بمستويات مهارة مشابهة  
د) استخدام ElastiCache أمام RDS لتحقيق زمن استجابة دون مللي ثانية

**تلميح 1**: نمط الوصول هو "البحث عن لاعب محدد بالمعرف." أي مفتاح يجعل هذا فعّالاً؟

**تلميح 2**: أحد الخيارات يخلق نقطة ساخنة رهيبة. أي سمة لها كثافة منخفضة جداً؟

**تلميح 3**: DynamoDB تُقدّم أصلاً زمن استجابة بأرقام أحادية بالمللي ثانية.

**الإجابة**: ب

**الشرح**: استخدام `playerId` كمفتاح تقسيم يوزّع البيانات بالتساوي عبر الأقسام ويُتيح استعلامات فورية بحسب معرف اللاعب — وهو بالضبط نمط الوصول الموصوف. نموذج المستند المرن في DynamoDB يتعامل مع أحجام مخزون متغيرة دون تغيير المخطط.

**لماذا ليس أ؟** RDS Aurora مع نسخ القراءة يضيف تعقيداً ولا يزال ليس الخيار الطبيعي الأول لهذا النوع من البحث القائم على المفتاح بنطاق الألعاب.

**لماذا ليس ج؟** استخدام `level` كمفتاح تقسيم يخلق أقساماً ساخنة حادة — معظم الزيارات تذهب إلى المستوى 1 (لاعبون جدد) أو المستوى الأقصى (محاربون نشطون)، مما يترك الأقسام الأخرى خاملة.

**لماذا ليس د؟** السؤال يصف DynamoDB، لا RDS. إضافة ElastiCache أمام RDS يُدخل خدمتين جديدتين حين تُحلّ المشكلة بـ DynamoDB وحدها.

*SAA-C03 المجال: تصميم معماريات عالية الأداء — المهمة 3.3*

**تمرين 3 — تحدي المعمارية** *(اختياري)*

تُضيف Nimbus ميزة "المفضلة": يمكن للعملاء حفظ أصناف قائمتهم المفضلة وإعادة طلبها بنقرة واحدة.

صمّم جدول DynamoDB لهذه الميزة. ما مفتاح التقسيم؟ هل ستستخدم مفتاح ترتيب؟ كيف ستبدو بنية العنصر؟

ثم فكّر: ماذا يحدث إذا احتجت إلى إظهار "أعلى 100 صنف الأكثر تفضيلاً عبر جميع العملاء"؟ هل تستطيع DynamoDB الإجابة على ذلك بكفاءة؟ إذا لا، ما الذي ستضيفه للمعمارية؟

*(لا توجد إجابة صحيحة واحدة. الهدف هو التدرب على التصميم بحسب أنماط الوصول.)*

## مشهد ما بعد النهاية

كان Leo قد نقل القائمة إلى DynamoDB بنهاية الأسبوع. كانت القراءات سريعة. كان المخطط مرناً. أضاف شركاء المطاعم أي حقول معدّلات يريدونها.

كان يشعر بثقة في نفسه.

ثم نظرت Priya إلى لوحة تحكم المراقبة.

قالت: "Leo، كل تحميل صفحة يُجري سبعة وأربعين طلباً من DynamoDB."

أكد Leo: "واحد لكل مطعم. لأن العميل في صفحة التصفح الكامل."

"وكل واحد من هذه الطلبات يستغرق حوالي أربعة مللي ثانية."

أجرى Leo الحساب. سبعة وأربعون في أربعة. "هذا... مئة وثمانية وثمانون مللي ثانية فقط للقائمة. قبل التصيير."

"في كل تحميل صفحة."

"لكل عميل."

حدّق في الشاشة.

قال: "نحتاج إلى ذاكرة تخزين مؤقت."

في الفصل القادم: الطبقة بين تطبيق Nimbus وقاعدة بياناتها التي تجعل الاستعلامات البطيئة سريعة.
